{
  "name": "portoflio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "serve dist/",
    "start": "npm run build && serve dist/ --listen ${PORT}"
  },
  "dependencies": {
    "astro": "^4.8.6",
    "@astrojs/check": "^0.7.0",
    "typescript": "^5.4.5",
    "serve": "^11.3.2"
  }
}